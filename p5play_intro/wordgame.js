// variables goes here

function setup() {
    new Canvas(600, 500);
    background("skyblue");
}

function draw() {
    background("skyblue"); // clear();

    textAlign(CENTER, CENTER);
    textSize(36);
    text("Guess the hidden word!", width/2, 50);
    text("Guess the hidden word!", width/2, 50);
    text("Guess the hidden word!", width/2, 50);
}